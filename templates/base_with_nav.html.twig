{% extends 'base.html.twig' %}

{% block body %}
    <nav class="navbar navbar-expand-lg navbar-light bg-light mb-4">
        <div class="container-fluid">
            {# Removed Fiszki App text as requested #}
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                        <li class="nav-item">
                            <span class="navbar-text me-3">
                                Witaj, {{ app.user.userIdentifier }}!
                            </span>
                        </li>
                        <li class="nav-item">
                            <a class="btn btn-outline-danger" href="{{ path('app_logout') }}">Wyloguj</a>
                        </li>
                    {% else %}
                        <li class="nav-item">
                            <a class="btn btn-outline-primary" href="{{ path('app_login') }}">Zaloguj!</a>
                        </li>
                        {# Add registration link here later if needed #}
                        {# <li class="nav-item ms-2">
                            <a class="btn btn-primary" href="{{ path('app_register') }}">Zarejestruj</a>
                        </li> #}
                    {% endif %}
                </ul>
            </div>
        </div>
    </nav>

    <div class="container">
        {% block body_content %}{% endblock %}
    </div>
{% endblock %}
